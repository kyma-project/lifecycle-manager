resources:
- manager.yaml

generatorOptions:
  disableNameSuffixHash: true

configMapGenerator:
- files:
  - controller_manager_config.yaml
  name: manager-config
apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization
images:
- name: controller
  newName: europe-west3-docker.pkg.dev/sap-kyma-jellyfish-dev/operator-test/operator-images/template-operator
  newTag: 0.0.4

# Helm value overlay for the block-non-deployer-module-image-pull-secret-injection e2e test.
# Replaces the kustomize klm-patch that set
# --restricted-default-modules=deployer,not-deployer.
manager:
  restrictedDefaultModules:
    - deployer
    - not-deployer

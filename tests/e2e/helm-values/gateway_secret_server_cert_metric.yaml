# Helm value overlay for the gateway-secret-server-cert-metric e2e test.
# Replaces the kustomize klm-patch that set
# --istio-gateway-secret-requeue-success-interval=1s.
manager:
  requeue:
    success:
      istioGatewaySecret: 1s

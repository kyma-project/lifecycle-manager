# Helm value overlay for the skr-image-pull-secret-sync e2e test.
# Replaces the kustomize klm-patch that set
# --skr-image-pull-secret=some-special-image-pull-secret.
skrImagePullSecret:
  name: some-special-image-pull-secret

apiVersion: operator.kyma-project.io/v1alpha1
kind: Kyma
metadata:
  name: kyma-sample5
  namespace: default
spec:
  profile: production
  channel: stable
  sync:
    enabled: false
  modules:
    - name: kcp-module
      controller: kcp-operator

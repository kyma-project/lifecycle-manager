apiVersion: operator.kyma-project.io/v1alpha1
kind: Kyma
metadata:
  name: kyma-sample
  namespace: default
spec:
  profile: production
  channel: stable
  sync:
    enabled: true
  modules:
    - name: skr-module
    - name: skr-module2
    - name: kcp-module

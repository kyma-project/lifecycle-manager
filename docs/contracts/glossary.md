SKR -remote cluster modules are installed in.
Default-CR - The default Custom Resource (CR) that is used to define the configuration and behavior of the SKR (SAP Kyma Runtime) remote cluster modules.
Module-CRD - The Custom Resource Definition (CRD) for the API group used by the `Default-CR`
